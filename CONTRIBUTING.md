# راهنمای مشارکت

پیش از هر چیز بخش «اصول پایه» در README.md رو بخون.

## ۱. مدارک قابل قبول

- اسکرین‌شات پیامک/ایمیل/نوتیفیکیشن با **تاریخ و ساعت قابل‌مشاهده**.
- در صورت امکان، شماره فرستنده یا آدرس ایمیل/دامنه‌ی فرستنده.
- اگر ادعای نقض حریم خصوصی (مثل ردیابی یا اشتراک‌گذاری اطلاعات بدون رضایت) داری، سیاست حریم خصوصی یا شرایط استفاده‌ی سرویس که مغایرت رفتار با آن رو نشون بده هم مفید است (لینک یا اسکرین‌شات).
- اسکرین‌شات‌های ویرایش‌شده یا دست‌کاری‌شده پذیرفته نمی‌شوند؛ اگر ویرایش برای سیاه کردن اطلاعات شخصی انجام شده، در توضیحات ذکر کن که کدام بخش redact شده.

## ۲. قبل از آپلود: حریم خصوصی خودتو رعایت کن

قبل از ارسال اسکرین‌شات:
- شماره تلفن، ایمیل، نام کامل، یا هر شناسه‌ی شخصی خودت یا افراد دیگر (به‌جز کسب‌وکار تبلیغ‌کننده) رو سیاه (blur/redact) کن.
- فقط اطلاعات لازم برای اثبات ادعا رو نگه دار.

## ۳. نام‌گذاری پوشه و فایل

```
proof/<slug-کسب‌وکار>/۱.png
proof/<slug-کسب‌وکار>/۲.png
entries/<slug-کسب‌وکار>.md
```

`slug` یعنی نام کسب‌وکار به لاتین، حروف کوچک، با خط تیره به‌جای فاصله. مثلاً برای «فروشگاه نمونه» → `foroshgah-namoone`.

## ۴. قالب فایل entry

فایل `entries/_template.md` رو کپی کن و پر کن:

```markdown
# نام کسب‌وکار

- **حوزه فعالیت:**
- **کانال(های) تبلیغ گزارش‌شده:** (پیامک / ایمیل / تماس / اینستاگرام / ...)
- **نوع تخلف ادعایی:** (اسپم بدون رضایت / عدم امکان لغو اشتراک / اشتراک‌گذاری اطلاعات با ثالث / ...)

## شرح ماجرا
توضیح مختصر و واقعیت‌محور، بدون زبان اهانت‌آمیز.

## شواهد
| تاریخ | نوع مدرک | لینک |
|---|---|---|
| ۱۴۰۴/۰۵/۲۱ | اسکرین‌شات پیامک | proof/slug/1.png |

## پاسخ کسب‌وکار (در صورت وجود)
_اگر نماینده‌ی رسمی پاسخ داده، این‌جا با تاریخ درج می‌شود._
```

## ۵. فرآیند بررسی

1. Pull Request باز می‌شود.
2. یک نگهدارنده (maintainer) بررسی می‌کند که:
   - مدرک واضح و تاریخ‌دار است.
   - اطلاعات شخصی افراد ثالث سیاه شده.
   - لحن توضیحات واقعیت‌محور است، نه توهین‌آمیز یا اتهام حقوقی قطعی (مثلاً به‌جای «این شرکت کلاهبردار است» بنویس «این شرکت بدون رضایت پیامک تبلیغاتی ارسال کرده»).
3. در صورت تایید، merge می‌شود و جدول README به‌روزرسانی می‌شود.

## ۶. فرآیند پاسخ کسب‌وکار

اگر نماینده‌ی رسمی یک کسب‌وکار بخواهد توضیح یا اصلاحیه ارسال کند:
- می‌تواند Issue جدید با برچسب `business-response` باز کند و مدرک هویت خودش (مثلاً ایمیل با دامنه رسمی شرکت) رو ضمیمه کند.
- پاسخ در بخش «پاسخ کسب‌وکار» همان فایل entry درج می‌شود، بدون حذف گزارش اصلی.

## ۷. چیزهایی که پذیرفته نمی‌شوند

- شایعه یا گزارش بدون مدرک.
- اطلاعات شخصی افشا‌شده از افراد غیرمرتبط.
- زبان توهین‌آمیز، تهدیدآمیز یا نفرت‌پراکنی.
- گزارش‌هایی با انگیزه‌ی رقابت تجاری بدون مدرک واقعی (مثلاً کارمند سابق ناراضی بدون سند).
